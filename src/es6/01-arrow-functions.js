function square (num){
    return num*num;
}

const square = (num) =>{ // funcion Arrow function
    return num * num;
}

const square = num => num * num; // si solo se tiene un parametro en la funcion, podemos
                                // resumir la funcion y enviar el parametro sin abrir corchetes.

